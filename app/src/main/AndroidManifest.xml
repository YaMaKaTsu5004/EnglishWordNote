<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishwordnote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnglishWordNote">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BookEditActivity"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".WordsEditActivity"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".WordsIOActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".WordsOutputActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".MakeTestActivity"
            android:label="@string/app_name">
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>